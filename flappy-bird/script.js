const canvas=document.getElementById('gameCanvas'),ctx=canvas.getContext('2d');const startBtn=document.getElementById('startBtn'),scoreDisplay=document.getElementById('score'),highScoreDisplay=document.getElementById('highScore'),overlay=document.getElementById('overlay'),overlayTitle=document.getElementById('overlayTitle'),overlayText=document.getElementById('overlayText');let birdX=72,birdY=280,velocity=0,gravity=.55,lift=-9.5,pipes=[],pipeWidth=64,pipeGap=165,frame=0,score=0,highScore=Number(localStorage.getItem('flappyHighScore')||0),gameRunning=false,animation;const stars=Array.from({length:45},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.8+.4,a:Math.random()}));function flap(){if(!gameRunning){startGame();return}velocity=lift}function startGame(){birdY=280;velocity=0;pipes=[];score=0;frame=0;gameRunning=true;overlay.classList.add('hidden');startBtn.textContent='Restart game';updateHUD();cancelAnimationFrame(animation);update()}function update(){if(!gameRunning)return;drawBackground();velocity+=gravity;birdY+=velocity;drawBird();if(frame%105===0){const top=Math.random()*(canvas.height-pipeGap-170)+70;pipes.push({x:canvas.width,top,passed:false})}pipes.forEach(pipe=>{pipe.x-=2.8;drawPipe(pipe);if(!pipe.passed&&pipe.x+pipeWidth<birdX){pipe.passed=true;score++;updateHUD()}if(birdX+25>pipe.x&&birdX-10<pipe.x+pipeWidth&&(birdY-15<pipe.top||birdY+15>pipe.top+pipeGap))gameOver()});pipes=pipes.filter(p=>p.x+pipeWidth>0);if(birdY+18>canvas.height||birdY-18<0)gameOver();frame++;animation=requestAnimationFrame(update)}function drawBackground(){const g=ctx.createLinearGradient(0,0,0,canvas.height);g.addColorStop(0,'#202d67');g.addColorStop(1,'#52b8c1');ctx.fillStyle=g;ctx.fillRect(0,0,canvas.width,canvas.height);stars.forEach(s=>{ctx.globalAlpha=s.a;ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()});ctx.globalAlpha=1;ctx.fillStyle='rgba(255,255,255,.12)';ctx.beginPath();ctx.arc(55,120,58,0,Math.PI*2);ctx.arc(340,190,90,0,Math.PI*2);ctx.fill()}function drawBird(){ctx.save();ctx.translate(birdX,birdY);ctx.rotate(Math.min(Math.PI/4,Math.max(-Math.PI/5,velocity/12)));ctx.font='32px serif';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('🐦',0,0);ctx.restore()}function drawPipe(p){const grad=ctx.createLinearGradient(p.x,0,p.x+pipeWidth,0);grad.addColorStop(0,'#a9df57');grad.addColorStop(1,'#4caa68');ctx.fillStyle=grad;ctx.fillRect(p.x,0,pipeWidth,p.top);ctx.fillRect(p.x-5,p.top-18,pipeWidth+10,18);ctx.fillRect(p.x,p.top+pipeGap,pipeWidth,canvas.height);ctx.fillRect(p.x-5,p.top+pipeGap,pipeWidth+10,18)}function gameOver(){if(!gameRunning)return;gameRunning=false;cancelAnimationFrame(animation);if(score>highScore){highScore=score;localStorage.setItem('flappyHighScore',highScore);overlayTitle.textContent='NEW BEST!';overlayText.textContent=`Amazing flight — you scored ${score}` }else{overlayTitle.textContent='NICE TRY!';overlayText.textContent=`You scored ${score}. Ready for another flight?`}overlay.classList.remove('hidden');updateHUD()}function updateHUD(){scoreDisplay.textContent=score;highScoreDisplay.textContent=highScore}document.addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();flap()}});canvas.addEventListener('pointerdown',flap);startBtn.addEventListener('click',startGame);overlay.addEventListener('click',flap);updateHUD();
