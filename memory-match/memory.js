const b=document.querySelector('#board'),s=document.querySelector('#status');let first,lock,matched;function reset(){let vals=['🎮','🎮','🚀','🚀','⭐','⭐','👾','👾','🎲','🎲','🕹️','🕹️','🔥','🔥','💎','💎'].sort(()=>Math.random()-.5);first=null;lock=false;matched=0;b.innerHTML='';s.textContent='Matches: 0 / 8';vals.forEach(v=>{let c=document.createElement('button');c.className='cell';c.textContent='?';c.onclick=()=>flip(c,v);b.append(c)})}function flip(c,v){if(lock||c.textContent!=='?')return;c.textContent=v;if(!first){first={c,v};return}if(first.v===v){matched++;first=null;s.textContent=`Matches: ${matched} / 8`;if(matched===8)s.textContent='You matched them all!'}else{lock=true;setTimeout(()=>{c.textContent='?';first.c.textContent='?';first=null;lock=false},700)}}document.querySelector('#reset').onclick=reset;reset();