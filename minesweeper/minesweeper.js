const b=document.querySelector('#board'),s=document.querySelector('#status');function reset(){let mines=new Set();while(mines.size<10)mines.add(Math.floor(Math.random()*64));b.innerHTML='';let cleared=0;for(let i=0;i<64;i++){let c=document.createElement('button');c.className='cell';c.onclick=()=>{if(c.disabled)return;if(mines.has(i)){c.textContent='💣';s.textContent='Boom! Try again.';[...b.children].forEach(x=>x.disabled=true);return}let n=[i-9,i-8,i-7,i-1,i+1,i+7,i+8,i+9].filter(j=>mines.has(j)&&j>=0&&j<64).length;c.textContent=n||'·';c.disabled=true;cleared++;if(cleared===54)s.textContent='Board cleared!'};b.append(c)}}document.querySelector('#reset').onclick=reset;reset();